# Web runtime
PORT=3004
NODE_ENV=production

# Existing game database (transitional during Strapi migration)
DATABASE_URL=postgresql://bbtown:bbtown@127.0.0.1:5432/bbtown?schema=public

# Strapi CMS (Phase 2 wiring)
STRAPI_URL=http://127.0.0.1:1338
# Optional: only needed if Public role is locked down
# STRAPI_API_TOKEN=replace_with_read_token
